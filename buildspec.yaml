version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18

    commands:
      - echo installing nodejs...

  pre_build:
    commands:
      - echo installing dependencies...
      - npm install
  build:
    commands:
      - echo testing...
      # - echo building...
      - npm run build
  post_build:
    commands:
      - aws cloudfront create-invalidation --distribution-id E1YQ3N9PAON0EL --paths '/*'
artifacts:
  files:
    - "dist/*"
  discard-paths: no


CloudShell
Feedback
Language
© 202